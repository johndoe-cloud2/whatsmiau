# Infra config for ases only (HTTPS). Copy to aws-infra.ases.env and fill in.
# Used by: aws-create-stack.sh, aws-update-stack.sh (not push-prod or the app).

# ACM certificate ARN for whatsmiau.asesadmin.com (ases only; leave empty in aws-infra.foxy.env for foxy)
CERTIFICATE_ARN=arn:aws:acm:us-east-1:ACCOUNT:certificate/ID
